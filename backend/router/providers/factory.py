from backend.router.providers.base import BaseProvider

def get_provider(provider_name: str, base_url: str = None) -> BaseProvider:
    # Minimal stub just to prevent ImportErrors
    # The new architecture is ModelGateway in backend.model_gateway
    pass
