[
  {
    "id": "code_1",
    "task_type": "coding",
    "prompt": "Write a Python function `lru_cache_ttl(maxsize=128, ttl_seconds=60)` decorator that caches function results with both an LRU eviction policy and a per-entry time-to-live expiration. It must be thread-safe.",
    "language": "python",
    "expected_constructs": ["threading.Lock", "time.time", "OrderedDict"]
  },
  {
    "id": "code_2",
    "task_type": "coding",
    "prompt": "Implement a high-performance C++14 circular ring buffer template class `RingBuffer<T, Capacity>` with lock-free atomic head and tail pointers suitable for single-producer single-consumer telemetry logging.",
    "language": "cpp",
    "expected_constructs": ["std::atomic", "capacity", "memory_order"]
  },
  {
    "id": "code_3",
    "task_type": "coding",
    "prompt": "Fix the following buggy Python code:\n\ndef find_duplicates(items):\n    seen = set()\n    dups = []\n    for x in items:\n        if x in seen:\n            dups.append(x)\n        seen.add(x)\n    return dups\n\nEnsure items with multiple occurrences appear only once in dups while preserving first-duplicate order.",
    "language": "python",
    "expected_constructs": ["seen", "dups_set"]
  }
]
